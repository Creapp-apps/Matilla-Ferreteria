export interface Category {
  id?: number;
  name: string;
  description?: string;
}

export interface Supplier {
  id?: number;
  name: string;
  phone?: string;
  email?: string;
  contact_person?: string;
}
